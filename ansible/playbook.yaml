---
- name: Setup Kubernetes cluster locally
  hosts: master
  become: true
  roles:
    - k8s-setup
  tasks:
    - name: Show IP address
      ansible.builtin.debug:
        msg: "Default IP : {{ ansible_default_ipv4.address }}"

